debug: true

aspect:
  logging:
    enabled: true
  execution:
    time: 200

spring:
  jpa:
    properties:
      hibernate:
        generate_statistics: true
    show-sql: true

logging:
  level:
    root: DEBUG
    org:
      apache:
        kafka: INFO
