on:
  push:
    branches:
      - staging

jobs:
  test:
    name: JUnit
    uses: ./.github/workflows/test.yaml
    with:
      environment: staging
    secrets: inherit
  build:
    name: Docker
    uses: ./.github/workflows/build.yaml
    with:
      environment: staging
    secrets: inherit
    needs: test
  deploy:
    name: Fargate
    uses: ./.github/workflows/deploy.yaml
    with:
      environment: staging
    secrets: inherit
    needs: build
