on:
  push:
    branches:
      - feature/**
      - fix/**
      - hotfix/**

jobs:
  test:
    name: JUnit
    uses: ./.github/workflows/test.yaml
    with:
      environment: dev
    secrets: inherit
