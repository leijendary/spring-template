on:
  push:
    branches:
      - develop

jobs:
  test:
    name: JUnit
    uses: ./.github/workflows/test.yaml
    with:
      environment: dev
    secrets: inherit
  build:
    name: Docker
    uses: ./.github/workflows/build.yaml
    with:
      environment: dev
    secrets: inherit
    needs: test
  deploy:
    name: Fargate
    uses: ./.github/workflows/deploy.yaml
    with:
      environment: dev
    secrets: inherit
    needs: build
